
<!DOCTYPE html>
<html>
<head>
	<title>Employee Data</title>	
	<style type="text/css">
	table{
		border: 1px solid black;
  
}
a:link    {color:blue; background-color:transparent; text-decoration:none}
a:visited {color:blue; background-color:transparent; text-decoration:none}
a:hover   {color:blue; background-color:transparent; text-decoration:none}
a:active  {color:blue; background-color:transparent; text-decoration:none}
table{
	border-collapse: collapse;
	text-align: center;

}

	th {
    background-color: #4CAF50;
    color: white;
}

th,td{
	 padding: 15px;
	 border-bottom: 1px solid #ddd;
}
tr:hover {background-color: #f5f5f5}
tr:nth-child(even) {background-color: #f2f2f2}
</style>
</head>
<body>

<table width="100%">
	<tr>
		<th>
			Employee Id
		</th>
		<th>Name</th>
		<th>Experience</th>
		<th>Date of Joining</th>
		<th>status Value</th>
		<th>Status</th>
		<th colspan="3">Actions</th>
	</tr>
	<% @emps.each do |emp| %>
	<tr>
		<td>
			<%= emp.e_id %>
		</td>
		<td><%= emp.name %></td>
		<td><%= emp.exp %></td>
		<td><%= emp.join_date %></td>
		<td><%= emp.status %></td>
		<td><%= link_to status_text(emp.status), status_employee_path(emp.id), method: :post %></td>	
		<td><%= link_to 'delete', employee_path(emp.id), method: :delete %></td>	
		<td><%= link_to 'edit', edit_employee_path(emp.id) %></td>
		<td><%= link_to 'show', employee_path(emp.id) %></td>
	</tr>
	<% end %>
</table>
<%= link_to 'new', new_employee_path %>

</body>
</html>